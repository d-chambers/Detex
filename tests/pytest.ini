[pytest]
addopts = --strict
markers =
    test_case: Test cases, integration/system tests (entire workflow performed)
    case1: the first of the test cases

    unit_tests: Unit(ish) tests that don't depend on a test case


